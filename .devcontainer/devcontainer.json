{
  "name": "Config",
  "dockerComposeFile": "../docker-compose.yaml",
  "service": "django", // El nombre del servicio definido en docker-compose.yml
  //"workspaceFolder": "/workspace", // Ruta dentro del contenedor donde se montará tu código
  //"postCreateCommand": "",
  "forwardPorts": [8001],
  "customizations": {
    "vscode": {
      "extensions": []
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {
        "version": "latest"
    }
}
}
